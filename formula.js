// IKL / formula.js

export const IKL_FORMULA = `
dir(
  mir(
    doch(
      egal(
        mov(
          nodi - (
            use(target) user
          ) move
        ) dir
      ) nir
    ) user
  )
) = REAL
`;

